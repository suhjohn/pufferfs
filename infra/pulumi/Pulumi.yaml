name: pufferfs
runtime:
  name: nodejs
  options:
    packagemanager: npm
description: Railway infrastructure for PufferFS API, NATS JetStream, and workers.
packages:
  railway:
    source: pulumi/pulumi/terraform-provider
    version: 1.1.3
    parameters:
      - terraform-community-providers/railway
      - 0.6.2
