services:
  api:
    environment:
      PUFFERFS_EMBEDDING_REQUESTS_PER_MINUTE: '12'
      PUFFERFS_EMBEDDING_TOKENS_PER_MINUTE: '32768'
  background:
    environment:
      PUFFERFS_EMBEDDING_REQUESTS_PER_MINUTE: '12'
      PUFFERFS_EMBEDDING_TOKENS_PER_MINUTE: '32768'
      PUFFERFS_WORKER_CONCURRENCY: '1'
